:root{
  --bg: #f3f4f6;
  --card: rgba(255,255,255,0.85);
  --ink: #0A2342;
  --muted: rgba(10,35,66,0.65);
  --shadow: 0 10px 24px rgba(0,0,0,0.06);
  --radius: 14px;
}

*{ box-sizing: border-box; }

.tv-body{
  margin: 0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background: var(--bg);
  color: var(--ink);

  min-height: 100vh;
  display: grid;
  grid-template-rows: auto 1fr; /* ✅ sin footer */
}

/* ----------------------------
   HEADER: Cliente+Hora (centro) / Logo (derecha) / Título
---------------------------- */
.tv-top{
  position: relative;
  padding: 14px 26px 10px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* bloque centrado */
.tv-client-block{
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

/* Cliente */
.tv-client{
  font-weight: 950;
  font-size: 20px;
}

/* Hora debajo del cliente */
.tv-time{
  font-size: 14px;
  color: var(--muted);
  font-weight: 850;
  margin-top: 4px;
}

/* Logo arriba derecha (más grande) */
.tv-client-logo{
  position: absolute;   /* ✅ antes estaba mal escrito */
  right: 26px;
  top: 10px;

  height: 140px;        /* ✅ cámbialo aquí para más grande */
  width: auto;
  object-fit: contain;

  display: none;        /* tv.js lo activa cuando hay logo */
}

/* Título centrado debajo */
.tv-title{
  margin: 18px 0 0;     /* ✅ separa del cliente/hora y baja un poco el título */
  text-align: center;
  font-size: 30px;
  font-weight: 900;
  letter-spacing: 0.2px;
}

/* ----------------------------
   MAIN / GRID
---------------------------- */
.tv-main{
  padding: 24px 26px 16px; /* ✅ más espacio arriba para que las cards bajen */
}

/* ✅ 4 por fila */
.tv-grid{
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 18px;
}

/* ✅ Cards creadas por tv.js */
.store-card{
  background: var(--card);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 12px;
  overflow: hidden;

  height: 255px;
  display: grid;
  grid-template-rows: auto 1fr auto;
}

.store-card.is-empty{
  opacity: 0.55;
}

/* Header de card */
.store-head{ margin-bottom: 6px; }

.store-name{
  font-weight: 900;
  font-size: 16px;
  margin: 2px 0 2px;
  color: var(--ink);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.store-sub{
  font-size: 12px;
  color: var(--muted);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Body: donut + stats */
.store-body{
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  align-items: center;
  min-height: 0;
}

.store-chart{
  position: relative;
  height: 170px;
  width: 100%;
}

.store-chart canvas{
  width: 100% !important;
  height: 100% !important;
}

/* Stats */
.store-stats{
  display: grid;
  gap: 6px;
  font-size: 13px;
}

.stat-row{
  display: flex;
  justify-content: space-between;
  gap: 10px;
  padding: 3px 6px;
  border-radius: 10px;
  background: rgba(255,255,255,0.55);
  border: 1px solid rgba(0,0,0,0.04);
}

.stat-row span:first-child{
  color: rgba(10,35,66,.80);
  font-weight: 800;
}

.stat-row span:last-child{
  font-weight: 900;
}

/* Footer de card */
.store-foot{
  margin-top: 8px;
  display: flex;
  justify-content: space-between;
  gap: 10px;
}

.pill{
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: 900;
  padding: 4px 10px;
  border-radius: 999px;
  background: rgba(10,35,66,0.08);
  color: rgba(10,35,66,.90);
  border: 1px solid rgba(10,35,66,0.08);
}

.pill-off{
  background: rgba(0,0,0,0.06);
  border-color: rgba(0,0,0,0.06);
  color: rgba(0,0,0,0.55);
}

/* ----------------------------
   Error overlay
---------------------------- */
.tv-error{
  position: fixed;
  inset: 0;
  display: none;
  align-items: center;
  justify-content: center;
  background: rgba(0,0,0,0.35);
  z-index: 9999;
}

.tv-error-box{
  background: rgba(255,255,255,0.95);
  border-radius: 16px;
  box-shadow: 0 20px 50px rgba(0,0,0,0.20);
  padding: 18px;
  width: min(520px, calc(100vw - 40px));
}

.tv-error-box h2{
  margin: 0 0 8px;
  font-size: 18px;
  font-weight: 950;
  color: var(--ink);
}

.tv-error-box p{
  margin: 0;
  color: rgba(10,35,66,.75);
  font-weight: 700;
  white-space: pre-line;
}