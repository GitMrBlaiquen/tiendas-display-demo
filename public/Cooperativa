<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SmartCountify TV</title>

  <link rel="stylesheet" href="/tv.css" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body class="tv-body">

  <!-- ✅ TÍTULO CENTRADO ARRIBA -->
  <header class="tv-top">
    <h1 class="tv-title">Contador de Personas Por Tiendas</h1>
  </header>

  <!-- ✅ GRID 4x3 -->
  <main class="tv-main">
    <section id="tvGrid" class="tv-grid" aria-label="Grid de tiendas"></section>
  </main>

  <!-- ✅ FOOTER ABAJO: Cliente izq + Logo der + Hora abajo -->
  <footer class="tv-footer">
    <div class="tv-footer-row">
      <div class="tv-client" id="tvClientLabel">Cliente: —</div>

      <div class="tv-client-logo-wrap">
        <img id="tvClientLogo" class="tv-client-logo" alt="Logo cliente" />
      </div>
    </div>

    <div class="tv-footer-row tv-footer-row--bottom">
      <div class="tv-time" id="tvClock">—</div>
    </div>
  </footer>

  <script src="/tv.js"></script>
</body>
</html>


:root{
  --bg: #f3f4f6;
  --card: rgba(255,255,255,0.85);
  --ink: #0A2342;
  --muted: rgba(10,35,66,0.65);
  --shadow: 0 10px 24px rgba(0,0,0,0.06);
  --radius: 14px;
}

*{ box-sizing: border-box; }

.tv-body{
  margin: 0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background: var(--bg);
  color: var(--ink);

  /* ✅ pantalla completa */
  min-height: 100vh;
  display: grid;
  grid-template-rows: auto 1fr auto;
}

.tv-top{
  padding: 18px 26px 8px;
}

.tv-title{
  margin: 0;
  text-align: center;
  font-size: 34px;
  font-weight: 900;
  letter-spacing: 0.2px;
}

.tv-main{
  padding: 10px 26px 6px;
}

/* ✅ 4 por fila (12 total = 3 filas) */
.tv-grid{
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 18px;
}

.tv-card{
  background: var(--card);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 12px;
  display: grid;
  grid-template-rows: auto 1fr auto;
  overflow: hidden;
}

.tv-card-title{
  font-weight: 900;
  font-size: 16px;
  margin: 2px 0 8px;
  color: var(--ink);
}

.tv-card-sub{
  margin-top: 8px;
  font-size: 12px;
  color: var(--muted);
  display: flex;
  justify-content: space-between;
  gap: 10px;
}

.tv-donut-wrap{
  position: relative;
  width: 100%;
  height: 180px; /* ajusta según TV */
}

.tv-footer{
  padding: 10px 26px 16px;
}

.tv-footer-row{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.tv-client{
  font-weight: 800;
  font-size: 18px;
}

.tv-client-logo-wrap{
  display: flex;
  justify-content: flex-end;
  align-items: center;
}

.tv-client-logo{
  height: 34px;
  width: auto;
  object-fit: contain;
  display: none; /* se muestra solo si hay logo */
}

.tv-footer-row--bottom{
  margin-top: 8px;
}

.tv-time{
  font-size: 14px;
  color: var(--muted);
  font-weight: 700;
}
